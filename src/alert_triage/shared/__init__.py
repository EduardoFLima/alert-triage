"""The shared kernel: vocabulary more than one context speaks.

Depends on no context, which is a contract rather than a discipline — anything
with a context-specific dependency cannot be put here, so this stays the small
set of primitives it is rather than becoming a dumping ground.
"""
