"""What investigation runs on: an agent framework, and a platform to ask.

Split by axis rather than lumped together: ``adk/`` is the framework and
``datadog/`` is the platform, so reaching another platform is another
directory rather than edits threaded through a shared one.
"""
