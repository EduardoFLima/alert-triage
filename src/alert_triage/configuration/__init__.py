"""Configuration: the settings a deployment behaves by, and how they are read.

A generic subdomain rather than a context with a published contract: nothing
reads configuration except to be configured by it, so every other context may
depend on this one and this one depends on none of them.
"""
